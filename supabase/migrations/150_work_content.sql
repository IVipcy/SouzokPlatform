-- ============================================================
-- 150_work_content.sql
-- 業務ごとのフリー作業内容（システム未定義の運用を吸収する余白）。
-- 業務キー（戸籍/不動産/手紙/執行通知 …）→ 自由記述。オーダーシート・各実務タブで共有。
-- ============================================================
ALTER TABLE cases ADD COLUMN IF NOT EXISTS work_content jsonb;
